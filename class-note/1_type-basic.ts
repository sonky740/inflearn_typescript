// JS 문자열 선언
const str = "hello";

// TS 문자열 선언
const str2: string = "hello";

// TS 숫자
let num: number = 10;

// TS 배열
let arr: number[] = [1, 2, 3]; // literal
let heroes: Array<string> = ["Capt", "Thor", "Hulk"];

// TS 튜플
let address: [string, number] = ["gangnam", 1];

// TS 객체
let obj: object = {};
let person: { name: string; age: number } = {
  name: "capt",
  age: 100,
};

// 진위값
let show: boolean = true;
